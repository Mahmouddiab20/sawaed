# 🎯 إعداد نظام العملاء المحتملين الحقيقي - سواعد

## 📋 نظرة عامة
هذا الدليل سيساعدك في إعداد لوحة التحكم لاستقبال العملاء المحتملين الحقيقيين من نماذج التواصل بدلاً من البيانات التجريبية.

## 🔧 الخطوة 1: إعداد قاعدة البيانات

### 1.1 تشغيل خادم MySQL
تأكد من تشغيل خادم MySQL على جهازك:
- **XAMPP**: تشغيل Apache و MySQL
- **WAMP**: تشغيل Apache و MySQL  
- **MAMP**: تشغيل Apache و MySQL

### 1.2 تحديث بيانات الاتصال
افتح ملف `config/database.php` وحدث البيانات:

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'sawaed_leads');
define('DB_USER', 'root'); // اسم المستخدم الخاص بك
define('DB_PASS', ''); // كلمة المرور الخاصة بك
```

### 1.3 إعداد قاعدة البيانات
1. افتح المتصفح واذهب إلى: `http://localhost/sawaed/setup-database.php`
2. اتبع التعليمات على الشاشة
3. ستتم إنشاء قاعدة البيانات والجداول تلقائياً

## 🚀 الخطوة 2: اختبار النظام

### 2.1 اختبار إرسال البيانات
1. افتح: `http://localhost/sawaed/test-lead-capture.html`
2. املأ النموذج وأرسل البيانات
3. تأكد من ظهور رسالة النجاح

### 2.2 فحص لوحة التحكم
1. افتح: `http://localhost/sawaed/admin/dashboard.php`
2. سجل الدخول باستخدام:
   - **اسم المستخدم:** `admin`
   - **كلمة المرور:** `sawaed2024`
3. يجب أن ترى البيانات الجديدة في الجدول

## 📊 ما ستحصل عليه

### ✅ بيانات حقيقية
- **عملاء محتملين حقيقيين** من نماذج التواصل
- **معلومات جغرافية** (البلد، المدينة)
- **تتبع الموافقة** على الخصوصية
- **إحصائيات دقيقة** ورسوم بيانية

### ✅ لوحة تحكم متقدمة
- **عرض جميع العملاء المحتملين**
- **تفاصيل كاملة** لكل عميل
- **إحصائيات جغرافية** وتوزيع العملاء
- **إدارة العملاء** (عرض، حذف)

## 🔍 استكشاف الأخطاء

### إذا لم تظهر البيانات في لوحة التحكم:
1. **تحقق من قاعدة البيانات** - تأكد من وجود بيانات في جدول `leads`
2. **تحقق من ملفات API** - تأكد من وجود `admin/api/get_lead_details.php`
3. **تحقق من الأخطاء** - افتح Console في المتصفح للبحث عن أخطاء JavaScript

### إذا فشل إرسال النماذج:
1. **تحقق من مسار API** - تأكد من وجود `api/contact_handler.php`
2. **تحقق من قاعدة البيانات** - تأكد من الاتصال بقاعدة البيانات
3. **تحقق من الصلاحيات** - تأكد من صلاحيات الكتابة في مجلد `api/`

## 🎯 اختبار سريع

### 1. اختبار إرسال نموذج
```bash
# افتح في المتصفح
http://localhost/sawaed/test-lead-capture.html
```

### 2. فحص قاعدة البيانات
```sql
-- افتح phpMyAdmin أو MySQL Workbench
SELECT * FROM leads ORDER BY created_at DESC LIMIT 5;
```

### 3. فحص لوحة التحكم
```bash
# افتح في المتصفح
http://localhost/sawaed/admin/dashboard.php
```

## 📈 الميزات المتقدمة

### تتبع جغرافي
- **تحديد البلد والمدينة** تلقائياً
- **إحصائيات جغرافية** مفصلة
- **خرائط توزيع العملاء**

### إدارة العملاء
- **عرض تفاصيل كاملة** لكل عميل
- **حذف العملاء** غير المرغوب فيهم
- **تصدير البيانات** (قريباً)

### أمان وخصوصية
- **تتبع الموافقة** على الخصوصية
- **حماية البيانات** الشخصية
- **امتثال GDPR** (قريباً)

## 🚀 الخطوات التالية

### 1. ربط النماذج الحقيقية
تأكد من أن نماذج التواصل في موقعك تستخدم:
```html
<form action="api/contact_handler.php" method="POST">
    <!-- حقول النموذج -->
</form>
```

### 2. تخصيص الإعدادات
يمكنك تخصيص الإعدادات في `config/database.php`:
- **مدة الاحتفاظ بالبيانات**
- **متطلبات الموافقة**
- **حدود معدل الطلبات**

### 3. إضافة المزيد من النماذج
يمكنك إضافة أنواع جديدة من النماذج:
- **نموذج التوظيف**
- **نموذج الاستشارة**
- **نموذج النشرة الإخبارية**

## 📞 الدعم

إذا واجهت أي مشاكل:
1. **تحقق من سجلات الأخطاء** في PHP
2. **تحقق من اتصال قاعدة البيانات**
3. **اختبر كل خطوة** على حدة
4. **راسلنا:** sawaedflow@gmail.com

---

**🎉 تهانينا!** لديك الآن نظام عملاء محتملين حقيقي يعمل بكفاءة!
